import 'package:flutter/material.dart';

class CreatePlayerViewBody extends StatelessWidget {
  const CreatePlayerViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
